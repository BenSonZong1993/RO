# 重构现状速查（v0.4.2 三层支柱架构）

> 更新：2026-08-29（第三轮：装备卡片战斗修正/命中公式/回收系统）。本文件为事实快照，所有结论均经浏览器实测。

## 架构状态
- **三层支柱**：`CharacterContext`（业务协调）→ Gateway（收费站/静态数据唯一入口）→ Repository（唯一持久化读写），数据流单向：UI → EventBus → Controller → Service → Context → Gateway → Repository → Persistence（PersistenceManager，localStorage v3 键）。
- **技能四层**：`SkillScheduler`（门面）→ `SkillRuntime`（状态机）+ `SkillStrategy`（决策）+ `SkillExecutor`（执行），数据一律经 `SkillGateway`（L0 基础+L1 机制补充+L2 补丁合并）。
- **轻量管理器**：`GroundEffectManager`（地面持续效果）、`SkillTriggerManager`（受击反射/击杀触发/普攻概率触发），均事件驱动、不动战斗循环核心。
- **乘区分层（第二轮规范）**：模式因子（ConfigProfiles 各组内 monster.exp/jobExp、char.expGain、drop.rate）∥ 真全局（`LevelPenaltyConfig` 等级差两表、`GlobalExpConfig` 需求侧倍率、`ResetServiceConfig` 重置费用）——相互独立、连乘不覆盖。结算点：`LootManager.processDeath`（获得侧 + MVP 结算）与 `CharService.getExpToLevel/getJobExpToLevel`（需求侧）。
- **城镇合成（第二轮）**：`TownMapConfig`（MapGroups.js，6 开放 + 22 注释隐藏）× `MapFlagData.TOWN_MAPS`（名单单一事实来源）→ `MapDataGateway` 合成 terrain=town 条目；下拉/备注/标题/SpawnManager 全部经网关消费。
- **跨职业继承（第二轮）**：`SkillService.learnSkill` 按 prevJobs 职业链解析所属树，历史树跳过 JobLv（`SkillGateway.canLearn(..., {skipJobLevel})`）；`UISkillTree` 链式分段渲染；转职保留 learnedSkills/技能点 + 回满（`CharacterContext.updateJob`）。
- **持久化扩容（第二轮）**：`ui.fearMvp`（畏战MVP魔物=Boss 类不刷新）、`ui.fearElite`（畏战精英魔物=Event 类不刷新，均在地图信息栏勾选）、`ui.autoSellEtc`（自动出售材料）、`ui.autoFarming`（挂机状态，刷新自动续战）写入 v3 ui 节。
- **战斗修正管线（第三轮补全）**：怪物单位携带 race/element/elementLevel/size；引擎消费 raceAddDamage/raceReduceDamage/elementalAddDamage/elementalReduceDamage/sizeAddDamage/sizeReduceDamage；命中公式 calcHitRate（80+HIT−FLEE 钳5~95）接入普攻/技能逐段判定，面板与怪物同标度（HIT=175+Lv+DEX+LUK/3，FLEE=100+Lv+AGI+LUK/5）。
- **回收系统（第三轮）**：`UIRecycle`（回收商人面板，主页面便捷按钮+普隆德拉 NPC）+ `ItemDataGateway.getSellPrice`（Sell 优先缺省 Buy/2）+ 战斗区"自动出售材料"勾选（ui.autoSellEtc → LootManager 落袋即售，仅材料类）。
- **背包五分类（第三轮）**：UIInventory 删除"全部"页，固定 武器/防具/饰品/消耗品/道具材料（卡片并入道具材料），默认武器。
- **云服务层（已预备 + ZeroTier 实测）**：`CloudStorageService` v2（本地优先 + write-behind 云同步 + 自动降级 + 服务器自动探测）+ `adapters/LocalStorageAdapter`（本地镜像）+ `adapters/CloudAdapter`（fetch 契约）+ `UINetStatus`（联网状态角标）。服务器：`server/server.js`（零依赖，文件持久化，静态托管游戏页——朋友直接开 http://虚拟IP:3000 即玩）+ `server/config.js`（全中文注释配置表：端口/限流/熔断/防作弊/排行）。服务器地址唯一维护点：`js/config/ConfigProfiles.js` 的 default.network.candidates。删档：设置按钮（云模式）→ 服务器存档+排行榜一并清除。
- **特殊机制**：全部经 `SKILL_PATCHES`（js/config/SkillPatches.js）的 `mechanism` 字段配置驱动，禁止硬编码进战斗核心。

## 已完成并实测通过的模块
- [x] 属性管线（8 处理器 + AttributeMediator 排队机制 + AttributeGateway 收费站）
- [x] 持久化统一（v2→v3 一次性迁移、旧键封存 `.migrated`；Repository → CloudStorageService → PersistenceManager）
- [x] 技能验收 10 类：单体/多段/范围/治疗/状态/被动/嘲讽/霸体/连击链/终极爆发（数值见《验证基准.md》）
- [x] 装备端到端：穿武器 finalATK +10（=武器 Attack，精确）；插卡 luk +2（=bLuk,2，精确）；拔卡还原
- [x] UI 闭环：技能树学习、装备栏/背包行点击详情弹窗（含卸下/装备/丢弃按钮）、商店/药水面板
- [x] 转生（配置累加 statPoints=48+52×n、扣费）/ 转职（条件树检查）
- [x] 排行榜面板（UIRanking：🥇🥈🥉 名次 + 当前角色高亮）+ 设置面板（UISettings：昵称修改/云端删档）
- [x] 昵称闭环：首弹/修改 ≤12 字符 → 存档 → 服务器排行榜显示昵称（非 ID）
- [x] 云服务 ZeroTier 实测：自动探测/自动注册/write-behind 推送/重启持久化/限流熔断/删档清排行/同源托管
- [x] 权限审计：AccessControl 白名单逐条有职责依据，无测试后门残留

## 待处理（详见《遗留问题清单.md》）
- [ ] 商店购买 UI 全点击流（底层 API 已验）
- [ ] `bFlee/bFlee2/bHit` 等 bonus 脚本字段解析缺口（EquipService simpleMap 未覆盖命中/闪避系）
- [ ] `char:changed` 监听器在怪物获状态时偶发空对象错误（被 EventBus try/catch 包住，非阻塞）
- [ ] 服务器排行榜 token 校验简化 / 防作弊待基线数据后启用（遗留清单 #15-16）
- [ ] 困难层技能机制（召唤/寻路阻挡/时停）——标记待专项

## 关键约束
- **禁止读取 >100KB 静态数据文件**（ItemData_*.js / SkillData.js / MonsterData.js / SkillGroups.js 等）。技能数据经 `SkillGateway.getSkillByAegis/getMergedSkillData` 单技能查询；物品经 `ItemDataGateway.getById`。
- 所有静态数据访问必须经对应 Gateway（规则 GATE-1）；角色写入必须经 Context/Repository（规则 CTX-1/ARCH-1）。
- 所有修改符合 AccessControl 权限表；测试期临时钩子/权限放开，结束后必须还原。
- 开发用本地静态服务器务必发 `Cache-Control: no-store`，否则浏览器缓存旧 JS 会造成"改了没生效"假象。



重构现状速查.md（更新）
markdown
# 重构现状速查（v5.0.0 + 普攻多段引擎）

> 更新：2026-08-31（双持拆分、二刀连击、普攻多段引擎）。

## 架构状态
- **三层支柱**：`CharacterContext`（业务协调）→ Gateway（收费站）→ Repository（持久化）。
- **普攻多段引擎**（新增）：`NormalAttackModifierEngine` 位于 `battle/` 层，处理所有 `onNormalAttack` 配置的修饰技能。
- **双持拆分**：在 `PlayerAI._buildBaseHits` 中实现，将合并后的 `finalATK` 按 `AS_LEFT` / `AS_RIGHT` 系数拆分为主/副手两段基础命中。
- **技能四层**：`SkillScheduler`（门面）→ `SkillRuntime`（状态机）+ `SkillStrategy`（决策）+ `SkillExecutor`（执行），数据经 `SkillGateway`。
- **配置驱动**：`SkillPatches.js` 包含双持补丁（`dual_wield`）和二刀连击（`onNormalAttack`）。

## 已完成并实测通过
- [x] 普攻多段引擎（支持 `extraHits` 类型）
- [x] 双持拆分（主/副手独立段，总伤害守恒）
- [x] 二刀连击（概率 7%~70%，触发额外段，不可暴击）
- [x] 双持伤害合并（`EquipService` 通过 `SkillGateway` 读取 `restoreRate`）
- [x] 装备系统修复（多槽位、头饰显示、饰品智能分配）

## 待处理（详见遗留问题清单）
- [ ] 状态光环技能机制（批量修复，暂缓）
- [ ] 召唤物、地形寻路等困难层功能
- [ ] 服务器排行榜 token 校验加固（公网前）

## 关键约束
- 所有普攻修饰技能必须通过 `SkillPatches.js` 配置 `onNormalAttack`，禁止硬编码在 `PlayerAI` 中。
- 静态数据访问必须经 Gateway（GATE-1）。
- UI 仅发送事件，不直接调用 Service（ARCH-2）。

## 新模块加载顺序
`NormalAttackModifierEngine` 在 `phase:9` 中，位于 `PlayerAI` 之前。

---

# 重构现状速查（追加：第二轮 · 放置节奏优化层）

> 更新：2026-09-03（battleSpeed 战斗加速档位 + 简洁飘字聚合）。四套冒烟 81 项全绿 + 页面实测通过。

## 本轮新增
- **战斗加速档位（1x/2x/4x）**：新模块 `js/core/BattleSpeedManager.js`（纯运行时状态：`setSpeed/getSpeed/getSpeeds/setCompact/isCompact/shouldAggregateFloatText/scaleDelta`，档位集合 [1,2,4] 配置于模块内）。`init.js` 游戏循环对四大战斗系统统一缩放 delta：`BattleController.update` / `SkillScheduler.update`（内含 SkillRuntime 的 GCD/冷却/咏唱状态机）/ `BattleEffectsManager.update` / `GroundEffectManager.update`。**只缩放时间维度，不触碰任何伤害数值路径**（冒烟 81 项全绿佐证）。页面实测采样：4s GCD 真实 1s 后剩 2.98/1.93/0（1x/2x/4x），10s 冷却剩 8.98/7.93/5.86，8s 咏唱进度 1.02/2.07/4.14——严格 2x/4x 比例。
- **简洁飘字聚合**：`BattleController._applyDamageToMonster` 中多段 hitResults 飘字，当 `battleSpeed>1x` 或勾选"简洁飘字"（`shouldAggregateFloatText()`）时合并为一条 `总伤×段数`（任一段暴击即暴击样式）；关键事件（经验/掉落/技能名/打断）不聚合。1x 也可手动勾选开启。
- **UI 控件**（index.html 地图信息面板）：`#sel-battle-speed`（1x 标准/2x 快速/4x 极速）+ `#chk-compact-float`（简洁飘字），init.js §8.7 接线，页面点击实测通过。
- **持久化**：v3 ui 节新增 `ui.battleSpeed`、`ui.compactFloat`（DataCoordinator dispatch 'init' → PersistenceManager 点路径），刷新自动恢复。
- **离线收益结算**：按 ROUND2 提示词延后至第 3 轮，本轮未做。

## 本轮顺带修复
- **server/server.js 静态缓存**：js/css 曾发 `public, max-age=31536000, immutable`（纯本地调试项目违反 no-store 规则），已改为**一律 `no-store`**。
- **index.html `window.APP_VERSION`**：21→22（配合上述缓存头修复，冲掉浏览器里 v21 immutable 旧缓存）。

## 实测方式注意（写入技能第 6/7 条）
- 页面实测：临时起 `node server/server.js` + ZCode 内置浏览器访问 localhost:3000（IAB 拦截 file://），验证完停服务器；禁止 computer-use 操控用户桌面浏览器。
- 改 JS 后页面行为像旧代码：先递增 `window.APP_VERSION` 再排查。
- 改文件一律用 Edit/Write 工具；PowerShell `Get-Content|Set-Content` 会按 GBK 解码损坏 UTF-8 中文（本轮曾损坏 index.html，靠用户 OneDrive 版本恢复）。

---

# 重构现状速查（追加：第三轮 · 装备精炼系统）

> 更新：2026-09-03（ROUND3 精炼粗链路全通）。smoke_refine 14 断言 + 四套旧冒烟 81 断言全绿 + 页面实测通过。

## 本轮新增（精炼系统）
- **配置表 `js/config/RefineConfig.js`**：maxLevel=10、safeLevel=3（≤3 强制成功不降级）、bonusPerLevel（武器 atk/matk 每级 +3；防具 def +1 / maxHp +30）、zenyCost[目标级]、orePerLevel[目标级]{weapon: Oridecon(984), armor: Elunium(985)}、successRate[目标级]、failureRule[当前级]{downgrade, breakChance}。全中文注释契约，神金属扩展位 TODO(ROUND4+) 标注。
- **服务 `js/services/RefineService.js`**（权限 `refine:perform`，白名单 RefineService/init/GMConsole；另入 data:inventory、char:deductZeny 白名单）：
  - `getRefineBonus(equipDef, refineLevel)` 纯函数 → {atk,matk,def,mdef,maxHp}，**与属性管线唯一接缝**
  - `refine(target)`：target={slot}（已装备）或 {stackKey}（背包）；判定→扣矿（MaterialService）→扣费（CharacterContext.deductZeny，失败不退）→写装备实例 `refine` 字段（随背包 v3 存档，无新键）；碎裂=移除实例+`refine:broken` 事件
  - `getRefineInfo(target)`：UI 确认弹窗数据源（成功率/费用/材料）
- **接缝改造 `EquipService.getEquipBonuses`**：删除旧 `refine*2` 写死逻辑，改调 getRefineBonus（武器→atk/matk，防具→def/mdef/maxHp），战斗核心零改动。
- **UI**：UIInventory 详情弹窗新增"精炼/精炼 +N"按钮（武器/防具类；已装备与背包物品均可）→ `ui:refine-item` → init.js 确认弹窗（成功率/Zeny/材料）→ RefineService。
- **冒烟 `js/tools/smoke_refine.js`**：14 断言（桩 InventoryRepository/CharacterContext/ItemDataGateway，含 Math.random 注入测降级/碎裂）。四套旧冒烟命令追加 `&& node tools\smoke_refine.js` 即 95 断言。
- **页面实测记录**：+1 长剑 → finalATK = 基线1 + 武器25 + 精炼3 = 29（精确）；刷新后 refine 持久。实测中发现 init.js 以 'init' 为 caller 调 refine 被权限表拒 → `refine:perform` 白名单加 'init'（与 rebirth/job 一致），已修。
- index.html APP_VERSION 22→23。
- ROUND4 附魔（EnchantService）按 RefineConfig+Service+getEquipBonuses 接缝同构旁置扩展，见 RefineService.js 文件头注释。

---

# 重构现状速查（追加：第四轮 · 装备附魔系统）

> 更新：2026-09-03（ROUND4 附魔粗链路全通）。smoke_enchant 16 断言 + 全套 111 断言全绿 + 页面实测通过。
> 设计参照：根目录《精炼与附魔的设计资料参考.txt》（剔除任务/NPC 外壳，保留框架内核）。

## 本轮新增（附魔系统）
- **配置表 `js/config/EnchantConfig.js`**：maxLevel=20（洗练+1 永不降级，城市间等级互通）、maxSlot=1、cityPools{prontera 基础属性 str/agi/vit/int/dex/luk，morroc 攻击 atk/matk，payon 种族增伤 raceBrute/raceDragon}、affixes{type:'attr'|'raceAdd', perLevel}、qualityWeights 白50/蓝30/紫15/橙5、qualityMult 1.0/1.5/2.0/3.0、zenyCost(level)=1000+level×500。全中文注释契约。
- **服务 `js/services/EnchantService.js`**（权限 `enchant:perform`，白名单 EnchantService/init/GMConsole；入 char:deductZeny 白名单）：
  - `getEnchantBonus(equipDef, enchant)` 纯函数 → {attrs, raceAddDamage}——与精炼同构的属性管线唯一接缝
  - `enchant(target, city)` 洗练：等级+1、随机重掷词条与品质；target={slot}|{stackKey}；返回 {success, level, affix, quality, cost, changed}
  - 装备实例数据结构：`enchant={city, level, affixId, quality}`，随背包 v3 存档无新键
- **装备实例扩展字段透传（框架级修复）**：`InventoryRepository.addItemRaw(..., extra)` 支持 enchant 透传（带 enchant 不参与堆叠合并）；`EquipService.doEquip/doUnequip` 穿脱双向透传 enchant；`getAllStacks` 投影含 enchant——修复"附魔穿脱丢失"漏洞。**精炼/将来任何装备实例扩展字段同走此通道。**
- **UI**：详情弹窗"附魔"按钮（时装不可）+ 词条展示块；init.js `ui:enchant-item`（确认弹窗→洗练→刷新）。粗版城市固定普隆德拉，三城池服务端就绪、UI 切换留 TODO（详见 `docs/ROUND4_PROGRESS.md` 未开始栏）。
- **冒烟 `js/tools/smoke_enchant.js`**：16 断言。全套冒烟命令（111 断言）：`node tools\smoke_multihit.js && node tools\smoke_executor.js && node tools\smoke_hooks.js && node tools\smoke_status.js && node tools\smoke_refine.js && node tools\smoke_enchant.js`
- **页面实测记录**：附魔 atk Lv1 白（+2）→ finalATK = 1+25+2 = 28 精确；穿脱后 enchant 保留；刷新后持久。index.html APP_VERSION 23→24。
- 精炼/附魔细节增强（保护石/祝福石、碎裂修复、精炼继承、觉醒、多词条槽、洗练材料、UI 城市切换）列入 `docs/ROUND4_PROGRESS.md` 未开始栏，供后续轮挑取。
- **下一轮入口**：`docs/ROUND5_PROMPT.md` = 精炼真实化（官方 `精炼-官方源码文件相关/refine.yml` 数值导入，字段契约不变）；附魔真实化（`附魔-官方源码文件相关/`，随机词条+品阶体系）留作 ROUND6。

---

# 重构现状速查（追加：第五轮 · 精炼系统官方数值真实化）

> 更新：2026-09-03（ROUND5 精炼官方数值全通）。smoke_refine 19 断言 + 全套六套冒烟 116 断言全绿 + 页面实测通过。
> 数据源：rAthena 官方 `精炼-官方源码文件相关/refine.yml`（Normal 档全量导入）。

## 本轮新增（精炼真实化）
- **RefineConfig 重构**：粗链条平铺表改为官方主表 `RefineConfig.table[group][物品等级]`（armor 物品等级 1~2 / weapon 1~4），每档含 safeLevel（官方 Rate=10000 段）、zeny（每次固定）、ore、bonus 累计表（下标=精炼等级）、successRate、failureRule。旧 bonusPerLevel/zenyCost/orePerLevel/successRate/failureRule 平铺键已删除，消费方仅 RefineService。
  - 关键官方规则：武器每级 +2/+3/+5/+7 ATK&MATK（按物品等级）；防具 +1 / +1.2 DEF（100 单位=1 DEF），无 MDEF/MaxHP 加成；高档普通精炼失败**必碎**不降级；防具物品等级 2 失败降 3 级不碎。
  - 物品等级字段：武器 `WeaponLevel`、防具 `ArmorLevel`（经 ItemData 实测，`RefineConfig.getItemLevel/getEntry` 统一取档，缺失按 1）。矿石实测 ID：Phracon 1010 / Emveretarcon 1011 / Oridecon 984 / Elunium 985 / Ethernium 1000331 / HD 6240·6241 / Enriched 7620·7619。
  - HD/Enriched 档字段已预留（oreHd/oreEnriched/zenyHd/zenyEnriched），Service 暂只消费 Normal（`_getOreRequirements` 的 costType 参数留位）。
- **RefineService 适配**：getRefineBonus 读累计 Bonus 表；safe=官方档与全局兜底 ≤3 取较大者；failureRule 支持 downgradeAmount；getRefineInfo 同步修正（曾残留旧 cfg.successRate 引用，实测发现已修）。
- **冒烟**：smoke_refine 扩至 19 断言；全套六套 **116 断言**：`node tools\smoke_multihit.js && node tools\smoke_executor.js && node tools\smoke_hooks.js && node tools\smoke_status.js && node tools\smoke_refine.js && node tools\smoke_enchant.js`
- **页面实测**：+1 长剑确认弹窗（100% / 50 Zeny / Phracon×1）与 7→8（60%）官方费率正确；finalATK = 1+25+2 = 28 精确；刷新持久；测试资产已还原。APP_VERSION 24→25。
- **事故记录**：index.html 曾被 PowerShell `Get-Content|Set-Content` 管道损坏（GBK 乱码，技能规则本就禁止）；用户经 OneDrive 版本历史恢复至 v21 后，AI 重新应用了 ROUND3/4/战斗加速轮的全部 index.html 改动（Refine/Enchant/BattleSpeedManager 挂载 + 战斗速度/简洁飘字控件 + APP_VERSION 25），143 个挂载脚本逐一核验存在。
- **未开始（后续轮）**：HD/Enriched 档 UI、官方 RandomBonus（+8 起随机浮动）、Blacksmith_Blessing(6635) 保护石、Carnium(6223)/Bradium(6224) 的 +11~20 档、精炼继承、影子装备精炼；附魔真实化 = ROUND6（`附魔-官方源码文件相关/`）。

---

# 重构现状速查（追加：第六轮 · 附魔系统官方数值真实化）

> 更新：2026-09-03（ROUND6 附魔官方数值全通）。smoke_enchant 21 断言 + 全套六套冒烟 **121 断言**全绿 + 页面实测通过。
> 数据源：rAthena 官方 `附魔-官方源码文件相关/`（enchantgrade.yml 品阶体系 + npc-enchantgrade.yml 兑换价 + 完整-item_enchant.yml 抽样）。

## 本轮新增（附魔真实化）
- **官方映射（同构参照 ROUND5 的 table 取档模式）**：
  - 品阶语义官方化：白/蓝/紫/橙 ↔ Grade None/D/C/B；`EnchantConfig.grades[]` 存官方 {label, official, bonus, fee, chances}，`getGrade/getUpgradeChance` 取档函数（概率按装备精炼等级取档，<9 取 Refine 9 行兜底、>20 钳 20——与 RefineConfig.getEntry 同构思路）。
  - 品阶数值：官方 Bonus 10/30/50/100 → `qualityMult = 1+Bonus/100` = 1.0/1.1/1.3/1.5（getEnchantBonus 公式形状不变，仅数值官方化）。
  - 升阶制替代随机权重：洗练时品阶**只升不降**，升阶概率取官方 Chances（蓝 10%/20%/60%/70%、紫 50%/60%、橙 40%/50%，按精炼分档）；升阶成功收官方 Etel 宝石兑换价（白→蓝 10 万 / 蓝→紫 20 万 / 紫→橙 30 万），失败不收；qualityWeights 弃用留档。
  - 每级洗练费 `1000+level×500` 保留（官方无逐级附魔费表，官方 slot Price 是逐槽一次性价）。
- **EnchantService**：`_gradeStep` 官方升阶判定；enchant 先判定后扣费；getEnchantInfo 增加 upgradeChance/gradeFee/nextGradeLabel（UI 数据源）。
- **UI 三城切换**（补 ROUND4 TODO）：init.js `_chooseEnchantCity()` 弹窗（普隆德拉基础属性/梦罗克攻击/斐扬种族增伤）→ 确认弹窗展示升阶概率与品阶费。
- **顺手修复**：smoke_refine ROUND5 遗留缺口——"武器 Lv3 档 5→6"断言未注入 Math.random（60% 成功率 → 全套冒烟偶发假红），已注入 0.1 确定化。
- **完整-item_enchant.yml 抽样结论（最大的未吃下官方资料点）**：138 组词条槽；属性珠 6 属均匀（各 9900），阶位概率 Tier1/2/3/特殊=9900/2500/400/234；逐槽 Price 10万~1亿+材料。**ItemData 探针实测：官方宝珠 Tier1/2/3 = 属性 +1/+2/+3，精炼 ≥7/≥9 各再 +1**（固定值+精炼联动模型，与本系统 perLevel×level 不同构，未导入）。探针脚本用后即删。
- **页面实测记录**：控制台直跑 enchant（Lv1 蓝 atk、1000+100000 扣费精确）；装备后 getEquipBonuses.atk=27（25+2）精确；穿脱透传保留；UI 全链路（详情弹窗→三城弹窗→斐扬确认→Lv2 紫 raceDragon、费用 201500 精确）；测试资产已还原（附魔清除、Zeny 回 7481）。APP_VERSION 25→26。
- **事故记录**：本轮曾违规用 PowerShell `-replace|Set-Content -Encoding UTF8` 改 index.html（指定了编码故未损坏，但属违禁操作模式）；规则重申：改文件一律 Edit/Write 工具，无论是否指定编码。
- 详细未开始栏见 `docs/ROUND4_PROGRESS.md` ROUND6 节（宝珠槽位系统、催化剂 Blessed_Etel_Dust、升阶失败碎裂惩罚、多词条槽等）。
